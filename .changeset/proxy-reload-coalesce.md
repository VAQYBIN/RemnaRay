---
'@remnaray/api': patch
---

`proxy-reloader` applies a reload requested while another is running right after it instead of dropping it, so a configuration written during a reload is not left unapplied.
