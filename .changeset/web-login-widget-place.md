---
'@remnaray/web': patch
---

Show the Telegram login button where the landing's «Войти» leads. The widget draws itself next to its script, which was appended to the end of the page, so «Войти» scrolled to an empty block; the script is now created inside it.
