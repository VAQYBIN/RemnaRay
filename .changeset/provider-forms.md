---
'@remnaray/api': patch
'@remnaray/web': patch
---

Configure payment providers with forms instead of JSON. The setup wizard and the console draw each provider's fields from its configuration schema, with labels and hints; the console can now edit, enable, rename and reorder providers after setup; a configuration the provider refuses names the field; and a secret left empty keeps its stored value.
