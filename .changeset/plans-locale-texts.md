---
'@remnaray/api': patch
---

Serve every plan's name and description in both locales. A plan created without a description stored `{}`, which the site's plan list refused, so `/account/plans` failed to load.
