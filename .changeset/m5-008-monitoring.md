---
---

Export the twelve section 9.9 metrics from `api`, `bot` and `worker`, reachable
only from the internal network, and ship an optional `monitoring` profile with
Prometheus and a provisioned Grafana dashboard.
