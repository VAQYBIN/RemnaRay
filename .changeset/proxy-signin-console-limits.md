---
---

Limit sign-in and the console apart at the proxy. Only the sign-in and TOTP requests spend the 5-a-minute allowance, the console reading its own session counts as console traffic, and the console allowance is 120 a minute with a burst of 120, so switching pages quickly is no longer answered 429.
