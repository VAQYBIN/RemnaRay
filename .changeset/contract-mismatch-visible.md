---
'@remnaray/domain': patch
'@remnaray/web': patch
'@remnaray/api': patch
---

Name an answer a page cannot read. It surfaced as «Код инцидента:» with nothing after it; the page now says the answer was unexpected and gives the request code, the console logs the fields that broke, and a server fault shows its incident id.
