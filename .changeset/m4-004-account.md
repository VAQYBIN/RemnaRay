---
'@remnaray/domain': minor
'@remnaray/api': minor
'@remnaray/web': minor
'@remnaray/bot': patch
---

Add the customer account API and pages: one `MeService` behind both the session
and acting-user surfaces, every section 13.4 page with loading, empty and error
states, the payment page, and the Telegram sign-in flow.
