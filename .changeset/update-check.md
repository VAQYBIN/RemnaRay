---
'@remnaray/api': minor
'@remnaray/worker': minor
'@remnaray/web': minor
---

Check GitHub Releases once a day and show "version X.Y.Z is available" on `/admin/system`, with a security badge when a release on the way fixes a vulnerability; `admin.check_updates` turns it off. The image now carries its release version, which `/admin/system` showed as 0.0.0.
